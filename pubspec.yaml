name: perspective_pageview
description: Easy to use 3D-Perspective PageView.
version: 1.0.0
homepage: https://github.com/retroportalstudio/flutter_perspective_pageview

environment:
  sdk: ">=2.16.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter
  provider: ^6.0.2

dev_dependencies:
  flutter_lints: ^1.0.4
  flutter_test:
    sdk: flutter

flutter:
